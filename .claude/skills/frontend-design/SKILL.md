All 6 skill files have been created. Here's a summary of what was generated:

**`.skills/frontend-design/SKILL.md`** (~130 lines) — Quick reference with page skeleton, card, button, form, CTA, and tag patterns. Includes a design validation checklist and links to all reference files.

**`references/aesthetics.md`** (~120 lines) — Full color token table with semantic usage, color pairing rules, hero gradient spec, typography table with font weights/sizes, font loading details, visual identity rationale, and iconography emoji mappings.

**`references/components.md`** (~140 lines) — Card variants (clickable, container, resource data-driven), all three button types (primary, secondary, ghost), tag styling, form input patterns, filter controls, and anti-patterns for mixing component classes.

**`references/layouts.md`** (~130 lines) — Layout chain documentation, page structure patterns with section alternation, all 4 grid systems (3-col, 2-col, 3-col-equal, 4-col footer), responsive breakpoint table, container width decision table, and anti-patterns for padding/width/spacing.

**`references/motion.md`** (~120 lines) — CSS-only transition inventory (card hover, button hover, form focus, mobile menu, nav links), animation philosophy for the caregiver audience, anti-patterns for layout animation and decorative motion, and a checklist for adding new animations.

**`references/patterns.md`** (~140 lines) — Color application DO/DON'T (custom properties vs Tailwind utilities), section alternation rules, full accessibility patterns (skip nav, ARIA roles, screen reader labels), CTA hierarchy with the amber-on-teal convention, anti-patterns (generic AI aesthetic, secondary color overuse, premature dark mode), and a comprehensive new page checklist with validation loop.

All examples are drawn directly from the actual codebase. Cross-references link to **tailwind**, **nunjucks**, **eleventy**, **javascript**, **crafting-page-messaging**, **tuning-landing-journeys**, and **adding-structured-signals** skills.